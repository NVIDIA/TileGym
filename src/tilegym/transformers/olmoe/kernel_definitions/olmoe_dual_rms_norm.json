{
  "axes": {
    "D": {
      "description": "Head dimension.",
      "type": "var"
    },
    "N": {
      "description": "Flattened leading dimensions.",
      "type": "var"
    }
  },
  "description": "OLMoE fused in-place RMSNorm for projected query and key tensors.",
  "inputs": {
    "eps": {
      "dtype": "float32",
      "shape": null
    },
    "k": {
      "dtype": "bfloat16",
      "shape": [
        "N",
        "D"
      ]
    },
    "k_weight": {
      "dtype": "bfloat16",
      "shape": [
        "D"
      ]
    },
    "q": {
      "dtype": "bfloat16",
      "shape": [
        "N",
        "D"
      ]
    },
    "q_weight": {
      "dtype": "bfloat16",
      "shape": [
        "D"
      ]
    }
  },
  "name": "olmoe_dual_rms_norm",
  "op_type": "dual_rmsnorm",
  "outputs": {
    "k_out": {
      "dtype": "bfloat16",
      "shape": [
        "N",
        "D"
      ]
    },
    "q_out": {
      "dtype": "bfloat16",
      "shape": [
        "N",
        "D"
      ]
    }
  },
  "reference": "# Source: https://github.com/huggingface/transformers/blob/aad13b87ed59f2afcfaebc985f403301887a35fc/src/transformers/models/olmoe/modeling_olmoe.py#L263-L264\n# Source: https://github.com/huggingface/transformers/blob/aad13b87ed59f2afcfaebc985f403301887a35fc/src/transformers/models/olmoe/modeling_olmoe.py#L58-L63\nimport torch\n\ndef run(eps, k, k_weight, q, q_weight):\n    qf = q.to(torch.float32)\n    kf = k.to(torch.float32)\n    q_out = qf * torch.rsqrt((qf * qf).mean(-1, keepdim=True) + eps) * q_weight\n    k_out = kf * torch.rsqrt((kf * kf).mean(-1, keepdim=True) + eps) * k_weight\n    return k_out.to(k.dtype), q_out.to(q.dtype)",
  "tags": [
    "model:olmoe",
    "stage:prefill",
    "stage:decode",
    "status:verified",
    "fused"
  ]
}
